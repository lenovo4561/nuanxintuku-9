<template>
  <div class="container">
    <!-- 背景图 -->
    <image class="bg" src="/assets/img/shangdian-bg.png"></image>
    
    <!-- 用户信息区域 -->
    <div class="user-section">
      <div class="user-info">
        <div class="avatar-wrapper">
          <image class="avatar" src="/assets/img/bg.png"></image>
        </div>
        <text class="username">周小艺</text>
      </div>
    </div>
    
    <!-- 其他服务 -->
    <div class="services-section">
      <text class="section-title">其他服务</text>
      
      <div class="service-card">
        <!-- 用户协议 -->
        <div class="service-item" onclick="openAgreement">
          <div class="service-left">
            <div class="icon-wrapper agreement-icon">
              <text class="icon-text">📄</text>
            </div>
            <text class="service-text">用户协议</text>
          </div>
          <text class="arrow-text">›</text>
        </div>
        
        <!-- 分割线 -->
        <div class="divider"></div>
        
        <!-- 隐私政策 -->
        <div class="service-item" onclick="openPrivacy">
          <div class="service-left">
            <div class="icon-wrapper privacy-icon">
              <text class="icon-text">🔒</text>
            </div>
            <text class="service-text">隐私政策</text>
          </div>
          <text class="arrow-text">›</text>
        </div>
      </div>
    </div>
    
    <!-- 底部导航栏 -->
    <div class="tab-bar">
       <div class="tab-item" onclick="switchTab('wallpaper')">
          <image class="tab-icon" src="/assets/img/bizhi-u.png"></image>
          <text class="tab-text">壁纸</text>
       </div>
       <div class="tab-item" onclick="switchTab('avatar')">
          <image class="tab-icon" src="/assets/img/touxiang-u.png"></image>
          <text class="tab-text">头像</text>
       </div>
       <div class="tab-item" onclick="switchTab('favorites')">
          <image class="tab-icon" src="/assets/img/shoucang-u.png"></image>
          <text class="tab-text">收藏</text>
       </div>
       <div class="tab-item" onclick="switchTab('mine')">
          <image class="tab-icon" src="/assets/img/mine-s.png"></image>
          <text class="tab-text active">我的</text>
       </div>
    </div>
  </div>
</template>

<script>
import router from '@system.router'
import webview from '@system.webview'

export default {
  private: {
    title: '我的',
    username: '周小艺',
    agreementUrl: 'https://api.51dsp.cn/index/agreement.do?pn=com.nuanxintkjs.nxheartwarming',
    privacyUrl: 'https://api.51dsp.cn/index/privacy.do?pn=com.nuanxintkjs.nxheartwarming'
  },
  onInit() {
    this.$page.setTitleBar({ text: '我的' })
  },
  openAgreement() {
    router.push({
      uri: '/pages/Web',
      params: {
        url: this.agreementUrl,
        title: '用户协议'
      }
    })
  },
  openPrivacy() {
    router.push({
      uri: '/pages/Web',
      params: {
        url: this.privacyUrl,
        title: '隐私政策'
      }
    })
  },
  switchTab(page) {
    if (page === 'wallpaper') router.replace({ uri: '/pages/Wallpaper' })
    if (page === 'avatar') router.replace({ uri: '/pages/Avatar' })
    if (page === 'favorites') router.replace({ uri: '/pages/Favorites' })
    // 已经在我的页，不需要跳转
  }
}
</script>

<style>
  .container {
    flex-direction: column;
    flex: 1;
    background-color: #0a1520;
  }
  
  .bg {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  
  /* 用户信息区域 */
  .user-section {
    padding-top: 80px;
    padding-bottom: 60px;
    padding-left: 60px;
  }
  
  .user-info {
    flex-direction: row;
    align-items: center;
  }
  
  .avatar-wrapper {
    width: 120px;
    height: 120px;
    border-radius: 60px;
    overflow: hidden;
    border-width: 3px;
    border-color: rgba(255, 255, 255, 0.3);
  }
  
  .avatar {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  
  .username {
    font-size: 42px;
    font-weight: bold;
    color: #ffffff;
    margin-left: 30px;
  }
  
  /* 其他服务区域 */
  .services-section {
    padding: 0 60px;
  }
  
  .section-title {
    font-size: 32px;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 30px;
  }
  
  .service-card {
    background-color: rgba(40, 60, 80, 0.6);
    border-radius: 30px;
    padding: 0 40px;
  }
  
  .service-item {
    height: 140px;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }
  
  .service-left {
    flex-direction: row;
    align-items: center;
  }
  
  .icon-wrapper {
    width: 60px;
    height: 60px;
    border-radius: 15px;
    justify-content: center;
    align-items: center;
    margin-right: 25px;
  }
  
  .agreement-icon {
    background-color: rgba(100, 200, 250, 0.3);
  }
  
  .privacy-icon {
    background-color: rgba(100, 200, 150, 0.3);
  }
  
  .icon-text {
    font-size: 36px;
  }
  
  .service-text {
    font-size: 34px;
    color: #ffffff;
  }
  
  .arrow-text {
    font-size: 50px;
    color: rgba(255, 255, 255, 0.5);
    font-weight: 100;
  }
  
  .divider {
    height: 1px;
    background-color: rgba(255, 255, 255, 0.1);
    margin-left: 85px;
  }
  
  /* 底部导航栏样式 */
  .tab-bar {
    height: 150px;
    width: 100%;
    background-color: rgba(20, 30, 40, 0.95);
    flex-direction: row;
    justify-content: space-around;
    align-items: center;
    position: fixed;
    bottom: 0;
    left: 0;
    border-top-width: 1px;
    border-top-color: rgba(255, 255, 255, 0.1);
  }
  
  .tab-bar .tab-item {
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 10px;
  }
  
  .tab-bar .tab-icon {
    width: 50px;
    height: 50px;
    margin-bottom: 8px;
  }
  
  .tab-bar .tab-text {
    font-size: 24px;
    color: rgba(255, 255, 255, 0.6);
  }
  
  .tab-bar .tab-text.active {
    color: #64c8b4;
  }
</style>
